# SlideIT - TODO List

## 🚨 긴급 수정 필요 (2025-01-21 최신)

### 1. 편집기 선택 창 취소 시 흰색 화면 문제 ❌❌❌
**문제:** 편집기 선택 창에서 취소 누르면 흰색 화면으로 가서 아무것도 누를 수 없음
**해결방안:**
- 취소 누르면 다이얼로그만 닫히고 이전 화면(프로필 또는 보관함)으로 정상 복귀
- Dialog는 기존 화면 위에 떠 있어야 함

### 2. 편집 방식 선택 모달 창 배경 문제 ❌❌❌
**문제:** 편집 방식 선택 모달 창 띄울 때 배경 창이 날아감
**해결방안:**
- 모달 창은 기존 화면 **위에** 떠야 함
- 배경 화면은 그대로 유지되어야 함
- CardEditor composable 구조 전면 수정 필요

### 3. 명함 저장이 안되는 문제 ❌❌❌
**문제:** 명함 저장 버튼이 작동하지 않음
**해결방안:**
- 저장 버튼 유효성 검사 확인
- 실제 저장 로직 작동 여부 확인
- 에러 로그 확인 필요

### 4. 내 명함이 보관함에 저장되는 문제 ❌❌❌
**문제:** "새 명함 만들기"로 저장하면 내 명함인데 명함 보관함에 들어감
**핵심:**
- **새 명함 만들기로 저장 = 내 명함 (isMyCard = true)**
- **내 명함은 명함 공유 창에만 표시**
- **명함 보관함은 받은 명함만 표시 (isMyCard = false)**
**해결방안:**
- MyCardEditor에서 저장 시 isMyCard = true 강제 설정
- CardStorageScreen은 isMyCard = false만 표시 (이미 구현됨)
- 저장 로직 재확인 필요

### 5. 저장 후 화면 이동 안됨 ❌❌❌
**문제:** 내 명함 저장 후 계속 편집 화면에 머물러 있음
**해결방안:**
- 저장 누르면 **최초 화면(명함 공유 화면)으로 즉시 이동**
- 내 명함은 한 번만 저장하면 됨 (계속 추가할 필요 없음)

### 6. 새 명함 만들기 뒤로가기 버튼 작동 안함 ❌❌❌
**문제:** 좌측 상단에 뒤로가기 버튼 있는데 클릭해도 반응 없음
**해결방안:**
- onNavigateBack 콜백 제대로 전달되는지 확인
- 버튼 클릭 이벤트 확인

---

## 이전 해결해야 할 문제들

### 1. 내 명함 편집 시 명함 보관함 저장 문제 ⚠️
**문제:** 내 명함 편집 시 명함 보관함에 자동으로 들어감
**해결방안:** 명함 보관함은 +버튼 눌러서 저장했을 때만 저장되어야 함
- `isMyCard` 플래그 확인 필요
- 내 명함과 보관함 명함 저장 로직 분리

### 2. 개인 설정 버튼 네비게이션 오류 ⚠️
**문제:** 개인 설정 > 개인 설정 눌렀을 때 명함 만들기 창으로 이동됨
**해결방안:** 개인 설정 창으로 이동해야 함
- `ProfileScreen.kt`의 네비게이션 로직 확인
- Settings 라우트 경로 수정

### 5. 명함 저장 형식 개선 필요 ⚠️
**문제:** 현재 명함이 데이터베이스에만 저장됨
**해결방안:** 명함을 두 가지 형태로 저장
1. **이미지 형태**: 명함 공유 화면의 입체 명함으로 표시 + 추후 공유 시 이미지 전송
2. **텍스트 파일**: 명함 정보를 JSON 또는 텍스트로 저장, 수정 시 기존 값 불러오기

**구현 계획:**
- 명함 저장 시 Composable을 Bitmap으로 캡처
- 이미지 파일을 내부 저장소에 저장 (`/data/data/com.example.slideit/files/cards/`)
- 데이터베이스에 이미지 경로 저장 (`thumbnailPath` 필드 활용)
- 명함 수정 시 기존 데이터를 불러와 폼 자동 채우기 (이미 구현됨)

**구현 우선순위:** 낮음 (추후 공유 기능 구현 시 함께 진행)

---

## 완료된 작업들 ✅

### 2025-01-21
- [x] 접속 시 에디터 모달 대신 명함 공유 화면 표시
- [x] 템플릿 추가 시 저장 버튼 활성화 문제 수정
- [x] CANVAS 에디터 준비 중 화면 추가
- [x] 새 명함 만들기 뒤로가기 기능 확인
- [x] ExperimentalMaterial3Api annotation 추가
- [x] 빌드 성공 (APK 생성 완료)

---

## 향후 계획 📋

### 공유 기능 (추후 구현)
- NFC 태그를 통한 명함 공유
- QR 코드 생성 및 스캔
- 이미지 파일로 명함 공유
- Bluetooth를 통한 근거리 공유

### 명함 관리 기능
- 명함 카테고리별 필터링
- 즐겨찾기 기능 강화
- 명함 검색 기능
- CSV 대량 가져오기/내보내기 개선

### UI/UX 개선
- 다크 모드 지원
- 애니메이션 효과 개선
- 명함 템플릿 다양화
- 상세 편집기(CANVAS) 구현

---

## 기술 스택

- **언어:** Kotlin
- **UI:** Jetpack Compose, Material3
- **아키텍처:** MVVM (ViewModel + Repository)
- **데이터베이스:** Room
- **이미지 처리:** Coil (예정)
- **OCR:** ML Kit Text Recognition
- **네비게이션:** Compose Navigation
- **비동기:** Coroutines + Flow

---

**마지막 업데이트:** 2025-01-21
